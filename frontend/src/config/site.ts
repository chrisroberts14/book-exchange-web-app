export type SiteConfig = typeof siteConfig;

export const siteConfig = {
  name: "Book Exchange Web App",
  description: "A web app for exchanging books",
  navItems: [
  ],
  navMenuItems: [
  ],
  links: {
  },
};
